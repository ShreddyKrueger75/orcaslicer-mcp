[project]
name = "orcaslicer-mcp"
version = "0.2.0"
description = "MCP server for OrcaSlicer: headless slicing, profile management, G-code analysis, and Elegoo Centauri Carbon printer control"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0",
    "pycentauri>=0.7",
]
